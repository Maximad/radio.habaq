import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset paths work both on the GitHub Pages project URL
  // (e.g. /radio.habaq/) and later behind a custom domain.
  base: './',
});
